apiVersion: apps/v1
kind: Deployment
metadata:
  name: supermario-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: supermario-app
  template:
    metadata:
      labels:
        app: supermario-app
    spec:
      automountServiceAccountToken: false
      containers:
      - name: supermario
        image: emekaezedozie276/devsecops-aks-pipeline:9
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
          requests:
            memory: "64Mi"
            cpu: "250m"
